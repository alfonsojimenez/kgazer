apiVersion: v1
kind: Service
metadata:
  name: {{ include "kgazer.fullname" . }}
  labels:
    {{- include "kgazer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kgazer.selectorLabels" . | nindent 4 }}
